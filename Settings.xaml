<ContentDialog
    x:Class="CountDown_Day.Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CountDown_Day"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="設定"
    PrimaryButtonText="關閉"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid x:Name="GROpen" Grid.Row="0" Width="Auto">
            <Button x:Name="BOpen" Content="打開設定文件夾" Click="BOpen_Click" Margin="7.5,7.5,7.5,7.5" HorizontalAlignment="Center"/>
        </Grid>
    </Grid>
</ContentDialog>
